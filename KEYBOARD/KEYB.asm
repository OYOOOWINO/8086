DATA SEGMENT
    PORTA EQU 00H
    PCW   EQU 06H 
DATA ENDS

STACK SEGMENT
    DW   128  DUP(0)
STACK ENDS

CODE SEGMENT
START:
    MOV AX, DATA
    MOV DS, AX
    MOV ES, AX  
    
    MOV DX,PCW
    MOV AL,10000000B
    OUT DX,AL
    MOV AL,27
    CALL OUTA 
 KEY:   
    MOV AH, 0H
    INT 16H
    CALL OUTA
    JMP KEY
    HLT
OUTA PROC 
;PARAMS: AL
	PUSH DX
	MOV DX,PORTA
	OUT DX,AL
	POP DX
	RET	
OUTA ENDP 
CODE ENDS
END START